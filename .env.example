# URL of the deployed chat backend (the server/ Express app), no trailing slash.
# Local dev (server running via `npm run dev` in server/): http://localhost:5000
# Production (Render): https://your-chat-server.onrender.com
VITE_CHAT_API_URL=http://localhost:5000
